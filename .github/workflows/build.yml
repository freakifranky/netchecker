name: Build NetChecker

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Build NetChecker.app (unsigned, for local testing)
        run: |
          xcodebuild \
            -project NetChecker.xcodeproj \
            -target NetChecker \
            -configuration Debug \
            -derivedDataPath build \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Ad-hoc codesign so it can launch
        run: |
          APP_PATH="build/Build/Products/Debug/NetChecker.app"
          codesign --force --deep --sign - "$APP_PATH"

      - name: Zip app for download
        run: |
          cd build/Build/Products/Debug
          ditto -c -k --sequesterRsrc --keepParent NetChecker.app NetChecker.zip

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: NetChecker-app
          path: build/Build/Products/Debug/NetChecker.zip
          retention-days: 14
